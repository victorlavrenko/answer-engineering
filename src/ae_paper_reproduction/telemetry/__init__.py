"""Downstream telemetry package.

Purpose:
    Provide the concrete implementation owned by this telemetry-related module.

Architectural role:
    Component inside the telemetry or telemetry-representation boundary.

Inputs:
    Adjacent telemetry and reporting collaborators.

Outputs:
    Downstream telemetry and reporting consumers.

Ownership:
    Owned by `answer_engineering.telemetry.__init__` within the downstream
    telemetry representation boundary.

"""

"""Inference stream I/O package.

Purpose:
    Group stream-session runtime modules.

Architectural role:
    Internal package boundary for stream I/O internals.

Import policy:
    Import directly from concrete stream I/O modules.

Boundary note:
    Marker-only package surface avoids duplicate import paths.

"""

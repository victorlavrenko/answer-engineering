"""Engine scoring logits package.

Purpose:
    Group logits-backed scoring implementation.

Architectural role:
    Internal package boundary for logits scoring internals.

Import policy:
    Import directly from concrete logits modules.

Boundary note:
    Marker-only package surface avoids extra indirection.

"""
